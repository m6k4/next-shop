const LastSeenProducts = () => {
  // const { lastSeenProducts } = useLastSeenProducts();

  return (
    <div>
      TEST
      {/* {lastSeenProducts.map((product) => (
        <ProductCard key={product.id} product={product} />
      ))} */}
    </div>
  );
}

export default LastSeenProducts;